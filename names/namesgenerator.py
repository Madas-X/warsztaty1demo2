# from Lib import random
from names import adjectives
from names import surnames


def get_random_name():
    # name = surnames.values[random.randrange(len(surnames.values))] + "_" + adjectives.values[
        # random.randrange(len(adjectives.values))]
    # if name == "boring_wozniak":
    #     name = get_random_name()
    return "boring_wozniak"
